version: '3.7'

services:
  app:
    build:
      context: .
      dockerfile: ./api/Dockerfile
      args:
        - SRC_PATH=api
  nginx:
    container_name: nginx
    build: nginx
    ports:
      - 8080:80
    depends_on:
      - app